{
  "name": "udaash",
  "version": "0.1.0",
  "description": "The Global Market Symphony - Next-gen market intelligence platform",
  "private": true,
  "license": "MIT",
  "author": "Udaash Team",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "start": "npm run start --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
